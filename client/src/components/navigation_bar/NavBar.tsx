import { NavWrapper } from "../../StyledComponents/NavBar.styled-components";
import TriBarMenu from "../trio-bar-menu/TriBarMenu";

const NavBar = () => {
  return (
    <NavWrapper>
      <TriBarMenu />
    </NavWrapper>
  );
};

export default NavBar;
